import BaseConverter from "@/components/BaseConverter";

export const metadata = {
  title: "Number Base Converter — Dev Toolkit",
};

export default function BasePage() {
  return <BaseConverter />;
}
