import Base64Converter from "@/components/Base64Converter";

export const metadata = {
  title: "Base64 Converter — Dev Toolkit",
};

export default function Base64Page() {
  return <Base64Converter />;
}
