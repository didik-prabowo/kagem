import YamlJsonConverter from "@/components/YamlJsonConverter";

export const metadata = {
  title: "YAML ↔ JSON ↔ .ENV — Dev Toolkit",
};

export default function YamlPage() {
  return <YamlJsonConverter />;
}
